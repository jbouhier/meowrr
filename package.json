{
  "name": "meowrr",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "tauri": "tauri",
    "test": "bun test",
    "seed": "bun run scripts/seed-stripe.ts",
    "check": "tsc --noEmit && bun run lint",
    "lint": "biome lint --error-on-warnings .",
    "format": "biome format --write .",
    "ci": "biome ci . && tsc --noEmit && bun test"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "@tauri-apps/plugin-opener": "^2.5.4",
    "@tauri-apps/plugin-updater": "^2.10.1",
    "@tauri-apps/plugin-process": "^2.3.1"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.4",
    "@tauri-apps/cli": "^2.11.4",
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2",
    "vite": "^8.2.1"
  }
}
