[package]
name = "meowrr"
version = "0.1.0"
description = "MeowRR Widget — Stripe MRR at a glance"
authors = ["JB Bouhier"]
edition = "2021"
rust-version = "1.77.2"

[lib]
name = "meowrr"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["macos-private-api"] }
tauri-plugin-window-state = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
